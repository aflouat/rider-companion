import { Typography } from '@mui/material';

const AdminDashboard = () => {
    return (
        <div>
            <Typography variant="h4">Admin Dashboard</Typography>
            {/* Add navigation or other admin features here */}
        </div>
    );
};

export default AdminDashboard;